export type ImageResponse = {
    id: string,
    prompt?: string,
    state?: string,
    create_time?: string,
    image_url: string,
    update_time?: string,
    idx: number,
    timestamp?: number
}